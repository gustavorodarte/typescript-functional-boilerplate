
type User {
    username: String
    email: String
    password: String
}

type Mutation {
    crateUser: (
        username: String!,
        email: String!,
        password: String!,
    ): User
}

type Query {
    userByUserName(username: String!): User
}